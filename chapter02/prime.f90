	program prime
	integer :: i
	integer :: j

	do i = 2, 10000
		if (.NOT.(mod(i,j)) break !if factor found, not prime
		if(j > (i/j)
	        if(!(i%j) exit; // if factor found, not prime
	        if(j > (i/j)) 
			print *, "is prime\n", i
	end do

	end program prime
